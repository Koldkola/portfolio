# Security and Environment Variables
# Copy this file to .env and fill in your values

# API
OPENAI_API_KEY=your_openai_api_key_here
PORT=3000

# CORS - Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://yourdomain.com

# Security Note:
# - All admin passwords use SHA-256 hashing with salt
# - Rate limiting: 5 failed password attempts per 15 minutes
# - File uploads: Max 5MB, only image formats (JPEG, PNG, GIF, WebP)
# - Content Security Policy prevents XSS attacks
# - All user inputs are sanitized to prevent injection attacks
# - Cryptominer detection runs automatically
